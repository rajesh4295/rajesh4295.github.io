export enum ThemeMode {
    LIGHT = 'light',
    DARK = 'dark'
};

export interface IThemeColors {
    primary: string;
    secondary: string;
    background: string;
    text: string;
    accent: string;
}